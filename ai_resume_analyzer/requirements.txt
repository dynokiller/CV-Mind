# ═══════════════════════════════════════════════════════
#  AI Resume Analyzer — Python Dependencies
#  V4.5: XGBoost + TF-IDF + OCR + LinkedIn + DistilBERT opt-in
# ═══════════════════════════════════════════════════════

# ── API Framework ──────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.6.0
pydantic-settings>=2.2.0
spacy>=3.7.0

# ── Deep Learning (optional transformer backend) ───────
# Kept minimal: only transformers is needed for DistilBERT
# zero-shot mode (USE_TRANSFORMER_CLASSIFIER=true).
# torch is large; install separately with appropriate CUDA wheel if needed.
torch>=2.2.0                   # CPU by default (no GPU needed for inference)
transformers>=4.38.0           # DistilBERT / MiniLM zero-shot classifier
datasets>=2.18.0               # HuggingFace Dataset for training pipeline
accelerate>=0.28.0             # Required by transformers Trainer
sentencepiece>=0.2.0           # Tokenizer backend
protobuf>=3.20.0               # HuggingFace tokenizer serialization

# ── SentenceTransformers (match score) ────────────────
sentence-transformers>=2.7.0

# ── XAI ───────────────────────────────────────────────
shap>=0.45.0

# ── OCR — Primary Engine ───────────────────────────────
pytesseract>=0.3.10            # Wrapper for Tesseract (system dep: see render.yaml)
Pillow>=10.2.0                 # Image loading + preprocessing
opencv-python-headless>=4.9.0  # Image processing (headless = no GUI, Render safe)

# ── OCR — Optional Better Accuracy Engine ─────────────
# Uncomment to enable EasyOCR (USE_EASYOCR=true env var).
# Downloads ~150 MB models on first use.
# easyocr>=1.7.0

# ── Resume Parsing ─────────────────────────────────────
pymupdf>=1.23.0                # PDF → text (fitz); also used for PDF page OCR
python-docx>=1.1.0             # DOCX → text

# ── Data / ML utilities ────────────────────────────────
scikit-learn>=1.4.0            # LabelEncoder, TF-IDF, metrics
numpy>=1.26.0
joblib>=1.3.0

# ── Advanced ML / Imbalance (V4 Pipeline) ──────────────
xgboost>=2.0.0                 # Primary domain classifier

# ── Logging ────────────────────────────────────────────
structlog>=24.1.0

# ── Testing ────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0                  # async test client for FastAPI

# ── Database ───────────────────────────────────────────
motor>=3.3.0                   # Async MongoDB driver
